<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectofinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoFinal">

        <activity
            android:name=".controller.RecView"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <!--  android:theme="@style/Theme.AppCompat.NoActionBar" elimina el actionBar de solo
             este activity, si se coloca en application, lo elimina de toda la aplicaion-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>